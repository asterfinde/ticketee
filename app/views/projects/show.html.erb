<% # This sets up a @title instance variable in the template. Because the template is rendered
	 # before the layout, you’re able to then use this variable in the layout.%>
<% title(@project.name, "Projects") %>
<%= link_to "Edit Project", edit_project_path(@project) %>
<%= link_to "Delete Project", project_path(@project), method: :delete,
	data: { confirm: "Are you sure you want to delete this project?" }
%>
<h1><%= @project.name %></h1>
<p><%= @project.description %></p>